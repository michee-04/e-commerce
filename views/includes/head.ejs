<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta http-equiv="X-UA-Compatible" content="ie=edge">
<title> </title>

<script src="https://cdn.jsdelivr.net/npm/flowbite@2.5.1/dist/flowbite.min.js"></script>

<link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.css"  rel="stylesheet" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.js"></script>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat&display=swap');

    /* Styles généraux */
    body {
        font-family: 'Montserrat', sans-serif;
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
    }

    .active {
        background-color: #475569;
        color: #FFFFFF;
    }

    /* Les boutons actives */
    html:not(.dark) .active {
        background-color: #0D522C;
        color: #FFFFFF;
    }

    /* Styles pour le mode clair uniquement */
    html:not(.dark) .aside-nav:hover {
        background-color: #3C7962;
        color: #FFFFFF;
    }

    html:not(.dark) .btn-add {
        background-color: #0D522C;
        color: #FFFFFF;
    }

    html:not(.dark) .btn-add:hover {
        background-color: #3C7962;
        color: #FFFFFF;
    }

    html:not(.dark) .btn-update {
        background-color: #3C7962;
        color: #FFFFFF;
    }

    html:not(.dark) .btn-update:hover {
        background-color: #0D522C;
        color: #FFFFFF;
    }

    


    .carousel-hide img {
        display: none; /* Cache complètement les images */
      }
      
      .carousel-hide h3 {
        opacity: 1; /* Rendre les titres visibles */
        transform: scale(1); /* Taille normale des titres */
        transition: opacity 0.3s ease, transform 0.3s ease;
        display: block;
        font-size: 16px; /* Taille des titres adaptée */
        font-weight: bold;
        text-align: center;
        color: #1E40AF; /* Couleur bleu similaire au design */
      }
      
      /* État normal où les images sont visibles */
      .carousel-show img {
        opacity: 1; /* Rendre les images visibles */
        transform: scale(1); /* Taille normale des images */
        display: block; /* Les images sont affichées */
        transition: opacity 0.3s ease, transform 0.3s ease;
      }
      
      .carousel-show h3 {
        opacity: 1; /* Titres visibles */
        transform: scale(0.8); /* Réduction de l'échelle */
        transition: opacity 0.3s ease, transform 0.3s ease;
        display: block;
        font-size: 25px; /* Ajustez la taille des titres si nécessaire */
        color: #1E40AF; /* Bleu pour les titres */
      }
      
      /* Conteneur en état normal */
      .carousel-show {
        height: auto; /* Hauteur ajustée automatiquement */
        overflow-x: auto; /* Autorise le défilement horizontal */
        display: flex; /* Disposition en flexbox */
        align-items: center; /* Centrage vertical des éléments */
        transition: height 0.3s ease;
      }
      
      /* Conteneur en état réduit */
      .carousel-hide {
        height: 50px; /* Hauteur fixe pour état réduit */
        overflow-x: hidden; /* Désactive le défilement horizontal */
        display: flex; /* Disposition en flexbox */
        justify-content: center; /* Centrage horizontal des titres */
        align-items: center; /* Centrage vertical des titres */
        gap: 20px; /* Espacement entre les titres */
        transition: height 0.3s ease, padding 0.3s ease;
      }
      
      /* Transition pour lisser les changements */
      .carousel-show,
      .carousel-hide {
        transition: height 0.3s ease, transform 0.3s ease;
      }
</style>
